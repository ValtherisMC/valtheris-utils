import { HexGeneratorTool } from '@/components/tools/hex-generator-tool';

export const metadata = {
  title: 'Hex Generator',
};

export default function HexGeneratorPage() {
  return <HexGeneratorTool />;
}
